# ARCCRAFT — scientific release candidate

This draft prepares `v1.0.0-paper`. It is not a published scientific release and has no archive DOI. The web application continues to report `CANDIDATE`.

Scientific freeze: `b642f8238d86b5ec3a4115d9d06d582682d69f1e`.
Application build: `cffc542a298189d2906e8939f47c2652c27527a7`.

The companion provides twenty English/French routes, a bounded Python API, separate reference/replay/exploration workflows, a filterable Failure Atlas, eight traceable claims, figures and a corrected 24-page manuscript. Original research files remain unchanged.

Deployment exclusions are restricted to root directories so that the registered publication copies remain downloadable. Post-deployment checks compare all fifty public downloads to their registered SHA-256 values.

The Windows reference environment reproduces the full-precision canonical output exactly. Linux differs on fifteen numeric fields by at most `2.220446049250313e-16`; its canonical replay remains **DIVERGENT**. The historical twelve-decimal fingerprint matches, and the motor replay is exact. No tolerance or replacement golden master has been introduced.

Validation includes Windows and Linux CI, the registered fifteen configuration/seed combinations, 32 browser checks across Chromium, Edge, Firefox and Playwright WebKit, and additional keyboard, zoom, cancellation and exploration isolation checks. These are not claims of manual screen-reader or physical Safari/iOS certification.

Assets include application and scientific source archives, canonical reference artefacts, publication sources, the corrected PDF, claim and download registries, checksums, the Python dependency lock, a web production SBOM and build evidence. The build evidence is a recorded receipt, not a signed supply-chain attestation. GitHub release immutability is enabled; the draft remains editable until publication.

Before publication, supply author metadata and reuse licences, reserve and verify the archive DOI, complete human review, regenerate citations/PDF and checksums, and retarget this draft if its final source commit changes. Do not publish the candidate assets unchanged as an already archived release.
